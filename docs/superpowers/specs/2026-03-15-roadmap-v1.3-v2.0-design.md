# Roadmap Design: v1.3.0 and v2.0.0

**Date:** 2026-03-15
**Status:** Draft
**Plugin:** simulink-automation-suite (fixed name)
**Baseline:** v1.2.1 (7 read-only actions, 91 tests, clean main branch)

---

## Strategy

Two releases following Strategy 2 (combined delivery):

| Version | Theme | Deliverables |
|---|---|---|
| **v1.3.0** | Consolidate + prepare | Shared layer extraction, module splitting, `find` action |
| **v2.0.0** | Write capability | New `simulink-edit` skill with `set_param` |

v1.3 restructures the codebase so v2.0 can build on clean shared infrastructure.
v2.0 is a major version because it introduces model mutation — a fundamentally new capability dimension.

---

## v1.3.0: Consolidate and Prepare

### 1. Shared Layer Extraction

#### Target Structure

```
skills/
  __init__.py              # NEW — required for skills._shared imports
  _shared/                 # No SKILL.md → plugin auto-discovery ignores it
    __init__.py
    session.py             # From sl_session.py: session discovery, connect, state
    validation.py          # From sl_core.py: validate_text_field, _validate_json_type, control char checks
    errors.py              # From sl_errors.py: make_error()
    json_io.py             # From sl_common.py: emit_json(), as_list(), JsonArgumentParser
  simulink_scan/
    (unchanged skill directory)
```

#### Why `_shared/` Is Safe

Plugin auto-discovery scans `skills/` for subdirectories containing `SKILL.md`.
`_shared/` has no `SKILL.md`, so it is invisible to the plugin system.
The underscore prefix further signals "internal library, not a skill."

#### Import Path Migration

```python
# Before (v1.2.x)
from .sl_common import emit_json
from .sl_errors import make_error
from .sl_session import connect_to_session

# After (v1.3.0)
from skills._shared.json_io import emit_json
from skills._shared.errors import make_error
from skills._shared.session import connect_to_session
```

#### `skills/__init__.py` Requirement

Currently `skills/__init__.py` does not exist. Python resolves `python -m skills.simulink_scan` via namespace packages, but explicit `skills._shared` imports require either:
- An explicit `skills/__init__.py` (recommended — avoids ambiguity), or
- Reliance on PEP 420 implicit namespace packages

Decision: add an empty `skills/__init__.py` in v1.3.0.

#### Shared Layer Scope (Thin and Stable)

Only skill-agnostic, rarely-changing code goes into `_shared/`:

| Module | Contents | Lines (est.) |
|---|---|---|
| `session.py` | `connect_to_session()`, `get_shared_sessions()`, `command_session_*()`, state file I/O | ~200 |
| `validation.py` | `validate_text_field()`, `_validate_json_type()`, control char / reserved char / whitespace / length checks | ~150 |
| `errors.py` | `make_error()` | ~10 |
| `json_io.py` | `JsonArgumentParser`, `emit_json()`, `as_list()` | ~30 |

Total: ~390 lines. Action routing, parser definitions, `_JSON_FIELD_TYPES`, and action implementations stay in each skill.

### 2. Module Splitting

#### Current State (v1.2.1)

| Module | Lines | Problem |
|---|---|---|
| `sl_core.py` | 805 | Exceeds 800-line soft limit |
| `sl_scan.py` | 835 | Exceeds 800-line soft limit |

#### Target State (v1.3.0)

```
skills/simulink_scan/scripts/
  sl_core.py          # Parser, _JSON_FIELD_TYPES, routing (~400 lines)
  sl_actions.py       # scan, inspect, list_opened, highlight (~350 lines)
  sl_connections.py   # connections graph traversal (~250 lines)
  sl_find.py          # find action (NEW, ~150 lines)
```

Split principle: `sl_scan.py` → `sl_actions.py` (general actions) + `sl_connections.py` (graph traversal is self-contained).
`sl_core.py` shrinks by extracting validation to `_shared/validation.py`.

### 3. `find` Action

#### Purpose

Search for blocks by name and/or type within a model or subsystem scope.
Fills the gap in current read-only workflow: agents can locate blocks without recursive scan + manual filtering.

#### Parameters (`_JSON_FIELD_TYPES` Entry)

| Field | Type | Required | Default | Description |
|---|---|---|---|---|
| `model` | string | No | null | Target model (same resolution as `scan`) |
| `subsystem` | string | No | null | Narrow search scope to a subsystem |
| `name` | string | No | null | Name substring match (case-insensitive, internally `(?i).*name.*` regex) |
| `block_type` | string | No | null | BlockType exact match (e.g., `SubSystem`, `Gain`, `PID Controller`) |
| `session` | string | No | null | MATLAB session name |
| `max_results` | integer | No | 200 | Output clipping |
| `fields` | array | No | null | Field projection |

**Constraint:** At least one of `name` or `block_type` must be provided. Otherwise → `invalid_input`.

**Input hardening:** `name`, `block_type`, `model`, `subsystem`, `session` all pass through `validate_text_field()` (control chars, reserved chars, whitespace, length ≤ 256).

#### MATLAB Implementation

```python
# Name-only (case-insensitive substring)
eng.find_system(scope, 'RegExp', 'on', 'Name', f'(?i).*{escaped_name}.*')

# Type-only
eng.find_system(scope, 'BlockType', block_type)

# Combined (AND semantics)
eng.find_system(scope, 'RegExp', 'on', 'Name', f'(?i).*{escaped_name}.*', 'BlockType', block_type)
```

Search scope resolved via existing `resolve_scan_root_path()`.

#### Output Format

```json
{
  "model": "my_model",
  "query": { "name": "PID", "block_type": null, "scope": "my_model" },
  "results": [
    { "path": "my_model/Controller/PID_speed", "name": "PID_speed", "type": "SubSystem", "parent": "my_model/Controller" },
    { "path": "my_model/Controller/PID_current", "name": "PID_current", "type": "SubSystem", "parent": "my_model/Controller" }
  ],
  "total_results": 2,
  "truncated": false
}
```

Projectable fields: `path`, `name`, `type`, `parent`.

#### Error Handling

- Model/session errors: reuse existing codes (`model_not_found`, `model_required`, `session_required`, `session_not_found`, `engine_unavailable`, `no_session`)
- Neither `name` nor `block_type` provided → `invalid_input`, `suggested_fix`: "Provide at least one of name or block_type."
- No results found → empty `results` array (not an error), `total_results: 0`

#### Recovery Routing (SKILL.md Addition)

```
- find returns empty results → broaden name pattern, try different block_type, or widen scope (remove subsystem constraint)
```

### 4. Documentation Synchronization (v1.3.0)

Per CLAUDE.md Change Synchronization rules, the following files must be updated:

| File | Changes |
|---|---|
| `sl_core.py` | Add `find` to `_JSON_FIELD_TYPES`, `build_parser()`, `run_action()` |
| `sl_find.py` | New file — action implementation |
| `README.md` | Add `find` to Core Actions table, Output Controls, JSON examples |
| `README.zh-CN.md` | Mirror English README changes |
| `SKILL.md` | Add `find` to Action Selection (item 5), Execution Templates, Recovery Routing |
| `reference.md` | Add Find Actions section, update Recovery Matrix |
| `test-scenarios.md` | Add find validation scenarios |

### 5. Test Plan (v1.3.0)

| Test File | Covers |
|---|---|
| `test_shared_session.py` | NEW — `_shared/session.py` unit tests |
| `test_shared_validation.py` | NEW — `_shared/validation.py` unit tests |
| `test_find_behavior.py` | NEW — find action with mocked MATLAB |
| `test_find_output_controls.py` | NEW — max_results clipping + fields projection |
| `test_schema_action.py` | UPDATE — verify `find` appears in schema output |
| `test_docs_contract.py` | UPDATE — verify `find` documented in SKILL.md, reference.md, test-scenarios.md |
| All existing tests | UPDATE — import path migration (no assertion changes) |

### 6. Version Bump

- `plugin.json` → `"version": "1.3.0"`
- `marketplace.json` → `plugins[0].version: "1.3.0"`
- Validate: `python -m unittest tests.test_plugin_manifest_contract tests.test_marketplace_manifest_contract -v && claude plugin validate .`

---

## v2.0.0: simulink-edit Skill

### 1. Skill Structure

```
skills/
  simulink_edit/
    __init__.py
    __main__.py             # python -m skills.simulink_edit → sl_core.main()
    SKILL.md                # Agent-facing instructions
    reference.md            # Deep reference with recovery matrix
    test-scenarios.md       # Validation scenarios
    scripts/
      __init__.py
      sl_core.py            # Edit-specific: parser, _JSON_FIELD_TYPES, routing
      sl_set_param.py       # set_param implementation
```

Shared dependencies from `skills._shared` (session, validation, errors, json_io).

### 2. SKILL.md Specification

#### Frontmatter

```yaml
---
name: simulink-edit
description: >-
  This skill should be used when the user asks to "modify Simulink block parameters",
  "set_param", "change parameter values", "edit Simulink model parameters",
  "update block settings", or needs to make parameter changes to Simulink models.
  Provides write workflows with mandatory dry-run preview and rollback support.
  This skill belongs to plugin simulink-automation-suite.
---
```

#### Body Structure (Imperative Form, Target ~1,500 Words)

Sections:

1. **Purpose** — Write-capable Simulink parameter editing. One skill inside `simulink-automation-suite`.
2. **Safety Model** — `dry_run` defaults to `true`. Always preview before applying. Persist rollback payload before executing.
3. **Preflight** — Same prerequisites as `simulink-scan` (MATLAB Engine, shared session). Discover contract via `schema`.
4. **Action Selection** — Currently: `set_param` for parameter modification, `schema` for contract discovery.
5. **Execution Templates** — Dry-run preview, execute with verification, rollback pattern.
6. **Recovery Routing** — Error code → next action mapping.
7. **Cross-Skill Workflow** — Use `simulink-scan` to find/inspect targets before editing. Use `simulink-scan inspect` after editing to verify.
8. **Related Docs** — Links to `reference.md`, `test-scenarios.md`.

### 3. `schema` Action (Runtime Self-Discovery)

Per agent-first-cli.md §2, `simulink-edit` must support its own `schema` action:

```bash
python -m skills.simulink_edit schema
python -m skills.simulink_edit --json '{"action":"schema"}'
```

Returns `_JSON_FIELD_TYPES` metadata for all edit actions, following the same structure as `simulink-scan schema`.

### 4. `set_param` Action

#### Parameters (`_JSON_FIELD_TYPES` Entry)

| Field | Type | Required | Default | Description |
|---|---|---|---|---|
| `target` | string | Yes | — | Full block path to modify |
| `param` | string | Yes | — | Parameter name |
| `value` | string | Yes | — | New parameter value (string form, MATLAB handles conversion) |
| `dry_run` | boolean | No | **true** | Preview mode — show diff without writing |
| `model` | string | No | null | Model disambiguation |
| `session` | string | No | null | MATLAB session name |

**Input hardening:** All string fields pass through `validate_text_field()`.

#### MATLAB Implementation

```python
# 1. Read current value
old_value = eng.get_param(target, param)

# 2. Dry-run: return diff, do NOT write
if dry_run:
    return { current_value, proposed_value, rollback }

# 3. Execute: write + read-back verification
eng.set_param(target, param, value)
verified_value = eng.get_param(target, param)
return { previous_value, new_value, verified, rollback }
```

#### Output Format

**Dry-run mode** (preview):

```json
{
  "action": "set_param",
  "dry_run": true,
  "target": "my_model/Controller/PID_speed",
  "param": "Kp",
  "current_value": "1.5",
  "proposed_value": "2.0",
  "rollback": {
    "action": "set_param",
    "target": "my_model/Controller/PID_speed",
    "param": "Kp",
    "value": "1.5",
    "dry_run": false
  }
}
```

**Execute mode** (actual write):

```json
{
  "action": "set_param",
  "dry_run": false,
  "target": "my_model/Controller/PID_speed",
  "param": "Kp",
  "previous_value": "1.5",
  "new_value": "2.0",
  "verified": true,
  "rollback": {
    "action": "set_param",
    "target": "my_model/Controller/PID_speed",
    "param": "Kp",
    "value": "1.5",
    "dry_run": false
  }
}
```

`rollback` is an executable JSON payload — persist it before proceeding to enable one-command undo.

#### Error Codes

| Error Code | Scenario |
|---|---|
| Reused | `engine_unavailable`, `no_session`, `session_required`, `session_not_found`, `model_not_found`, `block_not_found`, `invalid_input`, `unknown_parameter`, `invalid_json`, `json_conflict` |
| `param_not_found` | NEW — parameter name does not exist on the target block |
| `set_param_failed` | NEW — MATLAB `set_param` call raised an error |

Error envelope shape unchanged: `{error, message, details, suggested_fix?}`.

#### Safety Model

1. **`dry_run` defaults to `true`** — Agent must explicitly pass `dry_run: false` to write.
2. **Rollback payload in every response** — Both dry-run and execute responses include a `rollback` field.
3. **Read-back verification** — After `set_param`, the value is read back to confirm the write took effect.
4. **One parameter at a time** — No batch operations in v2.0. Each invocation changes one parameter on one block.

### 5. Agent Workflow Pattern

Typical read-understand-preview-write-verify cycle across both skills:

```
simulink-scan  list_opened           → discover models
simulink-scan  scan --recursive      → understand topology
simulink-scan  find --name "PID"     → locate target block
simulink-scan  inspect               → read current parameters
simulink-edit  set_param (dry_run)   → preview proposed change
simulink-edit  set_param (execute)   → apply change
simulink-scan  inspect               → verify result
```

### 6. Documentation (v2.0.0)

| File | Contents |
|---|---|
| `skills/simulink_edit/SKILL.md` | Agent-facing instructions (~1,500 words, imperative form) |
| `skills/simulink_edit/reference.md` | Deep reference: JSON contract, set_param examples, recovery matrix |
| `skills/simulink_edit/test-scenarios.md` | Validation scenarios for set_param |
| `README.md` | Add simulink-edit to plugin overview, core actions, examples |
| `README.zh-CN.md` | Mirror English changes |

### 7. Test Plan (v2.0.0)

| Test File | Covers |
|---|---|
| `test_edit_schema_action.py` | schema contract shape |
| `test_edit_json_input_mode.py` | JSON parsing, type checking |
| `test_edit_input_validation.py` | Field validation, input hardening |
| `test_set_param_behavior.py` | set_param with mocked MATLAB (dry-run and execute) |
| `test_set_param_dry_run.py` | Dry-run output format, rollback payload structure |
| `test_edit_error_contract.py` | Error envelope + new error codes |
| `test_edit_docs_contract.py` | Required doc sections present |
| `test_edit_module_entrypoint.py` | `python -m skills.simulink_edit` works |

### 8. Version Bump

- `plugin.json` → `"version": "2.0.0"`
- `marketplace.json` → `plugins[0].version: "2.0.0"`
- Git tag: `v2.0.0`

---

## Cross-Cutting Concerns

### CLAUDE.md Update

When v1.3.0 lands, update CLAUDE.md to reflect:
- Two module maps: `skills/simulink_scan/scripts/` and `skills/_shared/`
- Updated test map with new test files
- `find` action in commands/architecture sections
- Updated Change Synchronization rules for `_shared/` modules

When v2.0.0 lands, further update:
- Second skill `simulink-edit` in Project Identity and Architecture
- `simulink-edit` test map
- Safety model documentation

### Future Extensibility (Post-v2.0)

`simulink-edit` is designed for incremental expansion:

| Future Version | New Actions |
|---|---|
| v2.1.0 | `add_block`, `delete_block` (structural changes) |
| v2.2.0 | `add_line`, `delete_line` (signal routing) |
| v2.x.0 | `batch` mode (multiple operations in one call) |

Each new action follows the same pattern:
1. Add to `_JSON_FIELD_TYPES` in `sl_core.py`
2. New implementation file (e.g., `sl_structure.py`)
3. Register in `run_action()`
4. Update SKILL.md, reference.md, test-scenarios.md, README files
5. Add dedicated tests
6. Version bump

All write actions inherit the safety model: `dry_run` default true, rollback payload, read-back verification.

---

## Validation Checklist

### Before Committing v1.3.0

- [ ] `skills/__init__.py` exists
- [ ] `skills/_shared/` has `__init__.py`, `session.py`, `validation.py`, `errors.py`, `json_io.py`
- [ ] `skills/_shared/` has NO `SKILL.md`
- [ ] All existing tests pass with updated import paths
- [ ] `find` action in `_JSON_FIELD_TYPES`, parser, routing
- [ ] `find` input fields pass through `validate_text_field()`
- [ ] `find` output includes `total_results` and `truncated`
- [ ] Schema output includes `find` action
- [ ] README.md, README.zh-CN.md updated with `find`
- [ ] SKILL.md, reference.md, test-scenarios.md updated with `find`
- [ ] `test_docs_contract.py` enforces `find` presence
- [ ] `sl_core.py` ≤ 500 lines, all action files ≤ 500 lines
- [ ] Version bumped to 1.3.0 in plugin.json and marketplace.json
- [ ] `python -m unittest discover -s tests -p "test_*.py" -v` — all pass
- [ ] `claude plugin validate .` — pass

### Before Committing v2.0.0

- [ ] `skills/simulink_edit/` has `__init__.py`, `__main__.py`, `SKILL.md`, `reference.md`, `test-scenarios.md`
- [ ] `SKILL.md` frontmatter: third-person description with trigger phrases
- [ ] `SKILL.md` body: imperative form, ~1,500 words, references to reference.md
- [ ] `schema` action works: `python -m skills.simulink_edit schema`
- [ ] `set_param` dry_run defaults to true
- [ ] Response includes `rollback` payload (both dry-run and execute)
- [ ] Execute mode reads back value (`verified` field)
- [ ] `param_not_found` and `set_param_failed` error codes implemented
- [ ] All string fields pass through `validate_text_field()`
- [ ] Error envelope shape unchanged: `{error, message, details, suggested_fix?}`
- [ ] All edit tests pass
- [ ] README.md, README.zh-CN.md updated with simulink-edit
- [ ] `test_edit_docs_contract.py` enforces doc sections
- [ ] Version bumped to 2.0.0 in plugin.json and marketplace.json
- [ ] `python -m unittest discover -s tests -p "test_*.py" -v` — all pass
- [ ] `claude plugin validate .` — pass
